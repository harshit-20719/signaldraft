import { describe, it, expect } from "vitest";

// Day-1 placeholder: proves the test runner is wired up.
// Replaced by real engine tests (e.g. lib/pipeline/score.test.ts) in U6.
describe("test runner", () => {
  it("runs an example test", () => {
    expect(1 + 1).toBe(2);
  });
});
